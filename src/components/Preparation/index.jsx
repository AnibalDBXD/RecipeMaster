import styles from "../../styles/components/Preparation/Preparation.module.scss";

const Preparation = ({ children }) => <main className={styles.preparation}>{children}</main>;

export default Preparation;
